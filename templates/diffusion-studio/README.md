# Diffusion Studio project

Generated by ai-video-router. Claude Code is scoped to this directory and should iterate on `src/main.ts`.

## Entry points

- `index.html` — mounts the canvas and loads `src/main.ts`.
- `src/main.ts` — scripts the composition against the canvas.

## Conventions

- Diffusion Studio is browser-native — the composition renders live to a canvas. The dev server serves this HTML directly.
- The API evolves; if imports fail, check https://diffusion.studio and adjust.

## Scripts

- `pnpm dev` → Vite dev server (live preview at `/`).
- `pnpm build` → static build (preview only; MP4 export is via the SDK, not a CLI, so wire it up in code if needed).
